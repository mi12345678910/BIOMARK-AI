import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing to configure: the app is fully static with no server actions,
  // no API routes and no environment variables.
};

export default nextConfig;
