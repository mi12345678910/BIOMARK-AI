{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    /*
     * src/lib and src/data import each other with explicit .ts extensions so
     * the same files run unchanged under Node's --experimental-strip-types in
     * tests/. Safe here because noEmit is on — nothing is rewritten on output.
     */
    "allowImportingTsExtensions": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  /*
   * `tests/` is excluded because it runs under Node's --experimental-strip-types,
   * which requires explicit .ts import extensions that this compiler rejects.
   * Run it with `npm test`; it is not part of the Next build.
   */
  "exclude": ["node_modules"]
}
